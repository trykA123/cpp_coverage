name: Code Coverage

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]
  workflow_dispatch:

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: make deps

      - name: Build
        run: make build

      - name: Run tests
        run: ./main

      - name: Create coverage folder
        run: mkdir -p ${{ github.workspace }}/coverage

      - name: Generate Gcovr JSON report
        run: gcovr --json -o ${{ github.workspace }}/coverage/coverage-gcovr.json

      - name: Cobertura Action
        uses: 5monkeys/cobertura-action@master
        with:
          path: ${{ github.workspace }}/coverage/coverage-gcovr.json
          minimum_coverage: 35

      # - name: Publish Test Report
      #   uses: mikepenz/action-junit-report@v4
      #   if: success() || failure() # always run even if the previous step fails
      #   with:
      #     report_paths: ${{ github.workspace }}/coverage/coverage-gcovr.json
